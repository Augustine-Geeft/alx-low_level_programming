#include "main.h"
/**
 * _islower - Entry point
 * @c: input to be checked.
 * Return: 1 if lowercase, 0 if uppercase.
 */
int _islower(int c)
{
	c = islower('c');
	if (c > 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
