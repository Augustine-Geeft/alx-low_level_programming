#include "main.h"
#include <stdlib.h>
/**
 * _abs - Entry point.
 * @n: input to be checked.
 * Return: n is an absolute value.
 */
int _abs(int n)
{
	int c;

	c = abs(n);
	_putchar(c);
	return (0);
}
